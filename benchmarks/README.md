# Benchmark artifacts

This directory stores machine-readable benchmark evidence.

Planned files:

- `best.json` - best verified result, never merely the latest run;
- `history.jsonl` - append-only compact run summaries once the harness exists;
- `results/` - raw confirmation/final JSON or CSV artifacts;
- baseline artifacts generated by M2.

Rules are in `../BENCHMARK_CONTRACT.md`.
