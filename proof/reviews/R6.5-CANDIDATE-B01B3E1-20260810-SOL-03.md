# R6.5 provenance-corrected candidate review

Review ID: `R6.5-CANDIDATE-B01B3E1-20260810-SOL-03`  
Review type: R6.5 provenance-corrected candidate acceptance  
Reviewer role: `sol_advisor_advisor`  
Repository: PlaneFuse  
Head reviewed: `b01b3e14e03caa435dfd9523c2af782d55779299`  
Comparison/base commit: `aaebc7a05bcf0790d2789f8d5ea203f13ed324d1`  
Date UTC: 2026-08-10T23:19:48Z

## Verdict

`FIX-FIRST`

The implementation and raw rerun support a bounded negative result, but R6.5 was not yet an immutable accepted experiment.

## Findings

### F-001 — high — reproducibility

- Evidence: `git status --porcelain=v2` showed provenance-corrected proof artifacts modified and `proof/reviews/R6.5-CANDIDATE-AAEBC7A-20260810-SOL-02.md` untracked. `git show HEAD:proof/r6.5-camera-space.json` still contained generating commit `aaebc7a`, no environment object, and the old result.
- Risk: immutable HEAD did not contain the candidate being accepted.
- Required fix: commit corrected JSON, replay payload/manifest, sanitized log, review response, prior/current reviews, and updated ledgers as one coherent candidate.
- Closure evidence: `git show HEAD:proof/r6.5-camera-space.json` shows generating commit b01b3e1 and corrected hashes/statistics/environment; `git ls-files` lists the complete sequence; acceptance files have no diff.

### F-002 — high — claim ledger

- Evidence: `STATUS.md`, `CLAIMS.md`, and `EXPERIMENTS.md` still described fusion as awaiting authorization or not implemented, while the response already described the result.
- Risk: durable project state contradicted the implementation and review response.
- Required fix: preserve C025/E014, add the corrected negative result, update STATUS to R7 with C1 retained, and deny a speedup claim.
- Closure evidence: ledger entries cite the corrected artifact and commit, reproduce the negative statistics, and identify C1 as the R7 path.

### F-003 — high — source cleanliness provenance

- Evidence: `pf:100` and `pf:115` unconditionally supplied `PF_SOURCE_TREE_STATE="tracked source clean; declared user/evidence files excluded"`.
- Risk: the field could claim clean provenance while benchmark-relevant tracked files were modified.
- Required fix: derive source state from Git for an explicit benchmark-relevant path set or abort when those paths are dirty; record checked paths.
- Closure evidence: a dirty tracked path makes `./pf bench camera-space` fail, while the clean committed tree records the checked paths and clean state.

### F-004 — medium — structural verifier

- Evidence: `scripts/check_r6_5_camera_space_artifact.py` trusted stored batch/order counts and did not recompute batch membership, order balance, batch differences, aggregate percentage, bootstrap intervals, or generating commit.
- Risk: tampered structure or CI could still pass.
- Required fix: derive and verify all those values from raw records and deterministic bootstrap metadata, plus replay and commit provenance.
- Closure evidence: corrected artifact passes and tampering batch IDs/order/CI/percentage/replay/commit fails.

### F-005 — low — evidence portability

- Evidence: `proof/r6.5-camera-space-release.log` contained an absolute repository path.
- Risk: committed proof was not portable to a clean clone.
- Required fix: use repository-relative output paths.
- Closure evidence: `rg '/Users/|aarav' proof/r6.5-camera-space-release.log` returns no matches.

## Conclusion

The raw records contain five 200-pair batches with balanced ordering, matching replay hashes, passing parity, zero task disagreements, and a decisively negative B−C median and CI. After the provenance, ledger, dynamic-cleanliness, verifier, and log fixes, the result can be accepted only as a negative R6.5 experiment; accepted C1 remains the R7 candidate.
