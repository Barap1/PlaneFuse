# Response to R6.5 architecture checkpoint

Review: `R6.5-ARCHITECTURE-8040EFC-20260810-SOL-01`  
Original verdict: `FIX-FIRST`  
Response status: all actionable findings addressed and independently verified; fresh Sol re-review required before methodology acceptance.

## F-001 — direct fair B baseline

Implemented `MetalMobileNetV2CameraSpaceRGBPreprocessor` and `CameraSpaceNV12RGB.metal`. B now reads the retained source-plane textures with the accepted crop/nearest mapping, materializes planar Float32 RGB, and runs the unchanged B2 stem as the second ordered encoder in one caller-owned command buffer. C uses one direct native-stem command buffer. The old resize-then-B2 path remains available as historical/ablation code.

Closure evidence:

- `Tests/PlaneFuseCoreTests/CameraSpaceFusionMetalTests.swift` passed direct-B RGB parity and composed direct-B activation parity against the accepted resize-then-B2 path for identity and 640×480 crop cases, including byte extremes and bottom/right behavior.
- `proof/r6.5-camera-space.json` records one ordered candidate submission and one synchronization boundary per measured candidate frame; B RGB logical/allocated bytes are 602112/602112 and C RGB bytes are 0/0.
- The Release source replay result reports direct B p50 post-input-to-result `1.5609 ms` and direct C p50 `2.4017 ms`; the strongest measured B is retained and C is not declared a performance win.

## F-002 — source-resolution paired replay

Added `CameraSourceReplayBuffer` with canonical packed source rows, source dimensions, format, cadence, frame IDs, callback sequence, manifest hash, and payload hash. The R6.5 run captured 32 real 1920×1080 NV12 video-range frames and used that same persisted source replay for every alternating B/C pair. Separate physical-camera sessions are stored only as candidate-local descriptive measurements.

Closure evidence:

- `proof/r6.5-camera-source-replay.manifest.json` records 1920×1080 source dimensions, 32 frame IDs, callback sequence 2…33, and payload hash `fabc08678eb174d70a8dcd2c74fd089195e88c098c8ca914c26c5a1bb4437a2e`.
- `proof/r6.5-camera-source-replay.bin` is the hashed canonical source payload.
- `proof/r6.5-camera-space.json` records 5 batches × 200 direct pairs, raw frame IDs/order, and the same replay payload hash for both candidates. It separately records 300-frame B-only and C-only live sessions.

## F-003 — source texel quantization

Both direct kernels reconstruct source byte codes with `uint(value * 255.0f + 0.5f)` before the established video-range normalization. This matches `CameraNV12Resize.metal` and the accepted integer-plane C1/B2 inputs.

Closure evidence:

- The direct GPU parity test covers zero and 255 luma/chroma values, crop mapping, and bottom/right SAME cases.
- The Release first-frame gate reports direct C versus accepted C1 max activation absolute error `0.00000000`, direct B versus accepted B2 `0.00000000`, and accepted C top-1 agreement `1.0`.
- The full direct paired run reports maximum direct B/C activation error `8.344650268554688e-06`, top-1 agreement `1.0`, and zero recorded task disagreements.

## Parent judgment

The fixes close the methodology blockers. The camera-space fusion hypothesis itself is rejected as a performance candidate for this implementation: paired B−C p50 is `-0.9526 ms` with a positive CI for the opposite direction `[-0.9655,-0.9260]`, so C is slower. This result is qualified evidence only; no R6.5 speedup claim is accepted. Preserve C1 as the strongest C path for R7 unless a later final-evaluation matrix establishes another result under the stricter contract.
