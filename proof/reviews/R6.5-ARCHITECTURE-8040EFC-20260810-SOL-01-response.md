# Response to R6.5 architecture checkpoint

Review: `R6.5-ARCHITECTURE-8040EFC-20260810-SOL-01`  
Original verdict: `FIX-FIRST`  
Response status: all actionable findings addressed and independently verified; fresh Sol re-review required before methodology acceptance.

## F-001 — direct fair B baseline

Implemented `MetalMobileNetV2CameraSpaceRGBPreprocessor` and `CameraSpaceNV12RGB.metal`. B now reads the retained source-plane textures with the accepted crop/nearest mapping, materializes planar Float32 RGB, and runs the unchanged B2 stem as the second ordered encoder in one caller-owned command buffer. C uses one direct native-stem command buffer. The old resize-then-B2 path remains available as historical/ablation code.

Closure evidence:

- `Tests/PlaneFuseCoreTests/CameraSpaceFusionMetalTests.swift` passed direct-B RGB parity and composed direct-B activation parity against the accepted resize-then-B2 path for identity and 640×480 crop cases, including byte extremes and bottom/right behavior.
- `proof/r6.5-camera-space.json` records one ordered candidate submission and one synchronization boundary per measured candidate frame; B RGB logical/allocated bytes are 602112/602112 and C RGB bytes are 0/0.
- The Release source replay result reports direct B p50 post-input-to-result `1.5609 ms` and direct C p50 `2.4017 ms`; the strongest measured B is retained and C is not declared a performance win.

## F-002 — source-resolution paired replay

Added `CameraSourceReplayBuffer` with canonical packed source rows, source dimensions, format, cadence, frame IDs, callback sequence, manifest hash, and payload hash. The R6.5 run captured 32 real 1920×1080 NV12 video-range frames and used that same persisted source replay for every alternating B/C pair. Separate physical-camera sessions are stored only as candidate-local descriptive measurements.

Closure evidence:

- `proof/r6.5-camera-source-replay.manifest.json` records 1920×1080 source dimensions, 32 frame IDs, callback sequence 2…33, and payload hash `fabc08678eb174d70a8dcd2c74fd089195e88c098c8ca914c26c5a1bb4437a2e`.
- `proof/r6.5-camera-source-replay.bin` is the hashed canonical source payload.
- `proof/r6.5-camera-space.json` records 5 batches × 200 direct pairs, raw frame IDs/order, and the same replay payload hash for both candidates. It separately records 300-frame B-only and C-only live sessions.

## F-003 — source texel quantization

Both direct kernels reconstruct source byte codes with `uint(value * 255.0f + 0.5f)` before the established video-range normalization. This matches `CameraNV12Resize.metal` and the accepted integer-plane C1/B2 inputs.

Closure evidence:

- The direct GPU parity test covers zero and 255 luma/chroma values, crop mapping, and bottom/right SAME cases.
- The Release first-frame gate reports direct C versus accepted C1 max activation absolute error `0.00000000`, direct B versus accepted B2 `0.00000000`, and accepted C top-1 agreement `1.0`.
- The full direct paired run reports maximum direct B/C activation error `8.344650268554688e-06`, top-1 agreement `1.0`, and zero recorded task disagreements.

## Parent judgment

The fixes close the methodology blockers. The camera-space fusion hypothesis itself is rejected as a performance candidate for this implementation: paired B−C p50 is `-0.9526 ms` with a positive CI for the opposite direction `[-0.9655,-0.9260]`, so C is slower. This result is qualified evidence only; no R6.5 speedup claim is accepted. Preserve C1 as the strongest C path for R7 unless a later final-evaluation matrix establishes another result under the stricter contract.

## Candidate-review provenance closure (historical)

Sol’s subsequent candidate review (`R6.5-CANDIDATE-AAEBC7A-20260810-SOL-02`) identified a provenance blocker because the first result was generated before the harness commit. The complete harness was committed at `b01b3e1`, then the final provenance-corrected Release rerun was generated from `df3dccf`. The clean rerun is `proof/r6.5-camera-space.json`, with matching source replay artifacts and `scripts/check_r6_5_camera_space_artifact.py` passing. It records the required environment/model metadata and generating commit `df3dccf`; the promoted evidence is carried by a later descendant acceptance commit. The final rerun remains negative: paired B−C p50 `-1.4763 ms`, median 95% CI `[-1.5071,-1.4393]`, top-1 agreement `1.0`, accepted-path activation errors `0`, and zero task disagreements. The R6.5 result is accepted only as a qualified negative experiment, not as a performance claim.

## Response to R6.5-CANDIDATE-B01B3E1-20260810-SOL-03

Finding ID: F-001  
Disposition: valid  
Parent verification: HEAD `df3dccf` contains the final harness and provenance fixes; the generated artifact records that commit and is promoted into a later descendant acceptance commit.
Change/evidence: corrected rerun, review sequence, ledgers, and verifier are staged for the final acceptance commit.
Targeted validation: structural verifier plus `git show HEAD:proof/r6.5-camera-space.json` after rerun.  
Re-review requested: yes.

## Response to R6.5-ACCEPTANCE-9FCC2EB-20260810-SOL-04

Finding ID: F-003  
Disposition: valid; parent repair completed, fresh SHIP review pending.  
Parent verification: Sol identified both the incomplete hard-coded path set and the legacy live route that could assert cleanliness directly.  
Required change: `pf bench camera-space` is now the sole R6.5 benchmark entrypoint; the legacy `pf live --camera-space-bench` route is rejected. The checked-path set is derived from Git and includes `Package.swift`, `pf`, every tracked `Sources/**` file, and every tracked `Tests/**` file.  
Closure evidence: commit `a665fb4` makes `pf bench camera-space` the sole R6.5 entrypoint, derives 55 checked paths from `Package.swift`, `pf`, tracked `Sources/**`, and tracked `Tests/**`, and rejects `pf live --camera-space-bench`. The clean Release rerun records all 55 paths, the structural checker passes, and dirtying representative B pipeline, tail/bridge, compiler, statistics, and shader paths causes the benchmark to fail before execution.  
Re-review requested: yes, after evidence promotion.

Finding ID: F-002  
Disposition: valid; repaired in the project ledgers.  
Parent verification: STATUS, CLAIMS, and EXPERIMENTS now mark R6.5 acceptance pending while retaining C1 as the provisional R7 candidate.  
Acceptance evidence required: a subsequent immutable Sol review with `VERDICT: SHIP`.

Finding ID: F-002  
Disposition: valid  
Parent verification: STATUS, CLAIMS, and EXPERIMENTS now record the qualified negative and retain C1 for R7.  
Change/evidence: historical C026/E015 wording was superseded by the later full-source rerun; current STATUS remains pending until SHIP.  
Targeted validation: ledger inspection and final Sol review.  
Re-review requested: yes.

Finding ID: F-003  
Disposition: valid  
Parent verification: `pf` now derives benchmark-relevant source state from Git and aborts when those tracked paths are dirty.  
Change/evidence: `camera_space_source_state()` and `PF_SOURCE_TREE_CHECKED_PATHS`.  
Targeted validation: dirty-path fail-closed check and clean Release rerun.  
Re-review requested: yes.

Finding ID: F-004  
Disposition: valid  
Parent verification: the structural verifier now derives batch membership/order, summaries, aggregate percentage, deterministic bootstrap CI, hashes, and generating commit.  
Change/evidence: `scripts/check_r6_5_camera_space_artifact.py`.  
Targeted validation: clean artifact PASS plus tamper-failure checks.  
Re-review requested: yes.

Finding ID: F-005  
Disposition: valid  
Parent verification: the promoted log uses a repository-relative output path.  
Change/evidence: `proof/r6.5-camera-space-release.log`.  
Targeted validation: `rg '/Users/|aarav' proof/r6.5-camera-space-release.log` returns no matches.  
Re-review requested: yes.

## Response to R6.5-ACCEPTANCE-351E3D4-20260810-SOL-05

Finding ID: F-006  
Disposition: valid; correction in progress.  
Parent verification: the promoted artifact is the full-source rerun generated by `a665fb4`, not the historical `df3dccf` rerun. Its direct B/C activation maximum error is `8.58306884765625e-06`.  
Required correction: align C026, E015, and this response with the promoted artifact; label the earlier `df3dccf` discussion historical and remove the stale “STATUS advances to R7” wording. Keep acceptance pending until a fresh immutable Sol review returns `VERDICT: SHIP`.  
Acceptance evidence: current C026/E015 cite `a665fb4` and `8.58306884765625e-06`; STATUS, CLAIMS, and EXPERIMENTS remain pending; checker PASS; fresh SHIP review required.
