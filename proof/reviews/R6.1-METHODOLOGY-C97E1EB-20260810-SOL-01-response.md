# Parent response: R6.1 methodology review

Review: `R6.1-METHODOLOGY-C97E1EB-20260810-SOL-01`
Reviewed head: `c97e1eb` plus the current uncommitted methodology/statistics changes
Parent: Codex
Status: submitted for re-review; methodology is not accepted until `SHIP`

## F-001

Finding ID: F-001
Disposition: valid
Parent verification: Confirmed the original optional replay wording in `proof/r6.1-camera-benchmark-plan.md` and confirmed `Sources/PlaneFuseLive/main.swift` retained only the latest live frame without a persisted comparative replay.
Change/evidence: The plan now makes a persisted deterministic replay a mandatory precondition for comparative standalone throughput. It specifies resized NV12 Y/UV payloads, frame IDs/order, source PTS, callback sequence, pixel format, dimensions, cadence, binary payload plus manifest, SHA-256, identical configuration, and refusal on replay verification failure (`proof/r6.1-camera-benchmark-plan.md:74-92`).
Targeted validation: The implementation must produce matching Release B2-only/C1-only replay hashes and sequence manifests over at least 300 processed frames; this remains an implementation closure test, not yet a benchmark result.
Re-review requested: Yes; verify that the revised methodology is sufficiently binding before implementation proceeds.

## F-002

Finding ID: F-002
Disposition: valid
Parent verification: Confirmed the original plan combined serial B-then-C execution with callback-arrival timing and confirmed the R6 proof's fixed serial order (`proof/r6-camera-300-frame.json:42`).
Change/evidence: The plan now defines candidate-local `post_resize_input_to_result` as the paired headline metric, explicitly excludes the other candidate's work, labels serial frame-delivery values `order_contaminated`, stratifies them by order, and excludes them from paired headline differences and CIs (`proof/r6.1-camera-benchmark-plan.md:31-51`). It separately requires isolated B2-only/C1-only live delivery measurements.
Targeted validation: The implementation must persist per-pair order and all monotonic timestamps, and a targeted test/evidence check must show that predecessor-candidate time is absent from the paired headline boundary. This remains an implementation closure test, not yet a benchmark result.
Re-review requested: Yes; verify the revised boundary definitions before implementation proceeds.

## F-003

Finding ID: F-003
Disposition: valid
Parent verification: Confirmed the original plan omitted the bootstrap resampling unit, temporal-dependence treatment, seed/count, median definition, sign convention, and percentage denominator.
Change/evidence: The plan now preregisters nearest-rank p50/p95, arithmetic mean, nearest-rank MAD, the positive `B2-C1` sign, the aggregate percentage denominator, and a five-batch/10-frame-block/200-pair hierarchical bootstrap with 10,000 replicates and seed `0x50464A52` (`proof/r6.1-camera-benchmark-plan.md:53-72`). `Sources/PlaneFuseCore/BenchmarkStatistics.swift` implements the protocol and `Tests/PlaneFuseCoreTests/BenchmarkStatisticsTests.swift` locks the fixture CI values, summary definitions, sign convention, and invalid-input behavior.
Targeted validation: `BenchmarkStatisticsTests` passed all 4 tests through XcodeBuildMCP, including the fixed-seed expected intervals: mean CI `[89.34, 329.7]` and median CI `[14.0, 405.0]`.
Re-review requested: Yes; verify the preregistered algorithm and test evidence. The eventual benchmark JSON must still record the algorithm version, seed, count, batches, raw samples, and resulting interval.

## Parent acceptance boundary

The parent has independently verified all three findings and their revised evidence. No finding is being marked invalid or deferred. The R6.1 methodology remains blocked pending the read-only Sol re-review; a future `SHIP` review and then implementation evidence are required before accepting the benchmark milestone.

## Final methodology gate

The fresh independent `sol_advisor_advisor` review `R6.1-METHODOLOGY-C97E1EB-20260810-SOL-03` returned `VERDICT: SHIP` with no findings. The parent independently inspected the persisted verdict and current diff/status. This accepts the R6.1 methodology gate only; camera implementation, benchmark artifacts, and performance claims remain pending.

## F-004

Finding ID: F-004
Disposition: valid
Parent verification: Confirmed the re-review's mismatch: the plan said “at least five” batches while the helper required exactly five and accepted 10–199 samples per batch.
Change/evidence: The plan now binds the protocol to exactly five independent batches and exactly 200 measured pairs per batch. The helper rejects any other batch count or pair count with `invalidBatchCount`/`invalidPairCount` (`Sources/PlaneFuseCore/BenchmarkStatistics.swift`).
Targeted validation: Tests now cover the five-batch/200-pair production shape, six-batch rejection, and 199-pair rejection. The eventual artifact must show exactly five IDs and 200 measured pairs per ID.
Re-review requested: Yes.

## F-005

Finding ID: F-005
Disposition: valid
Parent verification: Confirmed that the prior implementation had no authoritative aggregate percentage function or algorithm-version identifier despite the plan and prior closure requirement.
Change/evidence: Added `BenchmarkStatistics.algorithmVersion` and `aggregatePercentage(pipelineB:pipelineC:)`, using the B2 p50 denominator and rejecting a zero baseline. The plan records the same version identifier.
Targeted validation: Tests now assert the exact 10% fixture result, version string, and zero-denominator rejection. The eventual benchmark JSON must serialize the version, p50 inputs, and percentage.
Re-review requested: Yes.
