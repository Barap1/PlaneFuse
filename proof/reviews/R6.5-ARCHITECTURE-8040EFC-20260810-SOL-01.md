# R6.5 architecture checkpoint — Sol review

Review ID: `R6.5-ARCHITECTURE-8040EFC-20260810-SOL-01`  
Review type: R6.5 architecture/methodology checkpoint  
Reviewer role: `sol_advisor_advisor`  
Repository: PlaneFuse  
Head reviewed: `8040efc8c2e92fefc233c0f3bbc1cccc94ab8e6b`  
Date UTC: 2026-08-10T22:40:03Z  
Isolation: behaviorally read-only; host permission profile was workspace-write, so OS-enforced read-only isolation is not claimed.

## Verdict

`FIX-FIRST`

The integer coordinate mapping is sufficiently specified and matches the existing shader equations. Implementation was blocked by baseline strength, source-resolution replay, and source-texel value semantics.

## Findings

### F-001 — high — fairness

- Evidence: `BENCHMARK_CONTRACT.md:7-15`, `BENCHMARK_CONTRACT_V2.md:38-49`, `Sources/PlaneFuseLive/CameraNV12MetalBridge.swift:127-143`, `Sources/PlaneFuseCore/MetalMobileNetV2RGBPipeline.swift:78-99`, and `Sources/PlaneFuseCore/Shaders/NV12MobileNetV2RGB.metal:43-79`. The proposed B sequence retains a standalone source-plane resize before NV12-to-planar-RGB conversion.
- Risk: A conventional B can apply the same nearest camera-space coordinates while materializing its required 224×224 planar Float32 RGB buffer. Retaining a resized-NV12 intermediate gives C a scheduling advantage that a strong B can remove.
- Required fix: Implement a conventional camera-space B preprocessor that reads source Y/UV textures using the accepted crop/resize mapping and writes materialized 224×224 planar Float32 RGB directly, followed by the unchanged ordinary RGB stem in the same ordered command buffer. Retain the three-stage B only as an ablation and select the strongest measured B.
- Closure evidence: parity between direct-camera B and accepted resize→B2 over procedural crop/chroma/padding cases and real frames; artifact fields proving one submission/one synchronization and the same tail; quick and confirmation evidence selecting the strongest measured B.

### F-002 — high — fairness and reproducibility

- Evidence: `Sources/PlaneFuseLive/main.swift:557-587` persists frames only after 224×224 resize; `proof/r6.1-camera-profiler-replay.manifest.json:2705-2709` records a 224×224 replay; `Sources/PlaneFuseLive/main.swift:664-704` pairs those resized textures; `BENCHMARK_CONTRACT_V2.md:40,182` requires matching source frames/order and the same captured frames or paired replay.
- Risk: The committed replay discarded original camera coordinates, so it cannot reproduce either source-camera path. Separate live sessions cannot support a paired camera-space claim.
- Required fix: Add a source-resolution NV12 paired replay, or retain each source CVPixelBuffer long enough to run alternating B/C on the identical frame. Preserve source dimensions, canonical row packing/strides, frame IDs, format, and payload hash. Keep R6.1 evidence immutable.
- Closure evidence: source-resolution manifest and payload; both candidates report identical source hash and frame IDs; alternating paired raw records over declared batches; structural artifact verification; separate live latency explicitly labeled non-paired/descriptive.

### F-003 — high — correctness

- Evidence: `Sources/PlaneFuseLive/CameraNV12MetalBridge.swift:107-116` creates `.r8Unorm`/`.rg8Unorm`; `Sources/PlaneFuseLive/Shaders/CameraNV12Resize.metal:18-19,30-33` converts reads using `uint(value * 255.0f + 0.5f)`; accepted C1 consumes integer textures in `Sources/PlaneFuseCore/Shaders/NV12MobileNetV2Stem.metal:8-9,23-26`. `Sources/PlaneFuseCore/CameraSpaceMapping.swift:133-181` proves coordinates only.
- Risk: A direct shader could omit or alter UNorm-to-byte quantization while still passing coordinate tests, changing activations.
- Required fix: Reconstruct Y/Cb/Cr integer codes with the same rounding before existing video-range normalization, or prove an equivalent operation. Apply identical source-value semantics to direct B and C.
- Closure evidence: GPU comparison against materialized resized Y/UV code planes over code extremes, chroma phases, crop boundaries, and SAME skips; end-to-end source-resolution activation parity at the established 1e-5 threshold plus task agreement on real replay frames.

## Parent response

This review is actionable and is not accepted as a ship decision. The parent will implement all three fixes, run targeted parity and structural tests, then request a fresh read-only review. No candidate performance claim is accepted from this checkpoint.
