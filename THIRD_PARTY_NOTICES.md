# Third-party notices

PlaneFuse source code is released under the MIT license in `LICENSE`.

The MobileNetV2 model and derived local Core ML assets retain their upstream Apple sample/model attribution and terms. The source hash, derivation inputs, and local asset lineage are recorded in the model manifests under `models/derived/` and `proof/`.

The R7 real-image corpus keeps per-sample source URL, license, and attribution fields in `proof/m5-validation-corpus.json`. Procedural stress inputs are generated locally and are labeled as such.

Before any public repository or submission release, review all model, image, font, and media terms again. No external video or Devpost publication is authorized by this repository state.
